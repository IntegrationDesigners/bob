

CREATE COMPUTE MODULE FixedLengthtoBLOB_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		
		SET OutputRoot.Properties = InputRoot.Properties;
		CREATE LASTCHILD OF OutputRoot DOMAIN('BLOB');
		DECLARE options INTEGER BITOR(FolderBitStream, ValidateNone);
		SET OutputRoot.BLOB.BLOB =
			ASBITSTREAM(InputRoot.DFDL.MessageFixedLength
				OPTIONS options
				CCSID InputRoot.Properties.CodedCharSetId);
		
		RETURN TRUE;
	END;

END MODULE;
